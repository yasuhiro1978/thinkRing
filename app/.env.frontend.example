# ============================================
# thinkRing Frontend Environment Variables
# ============================================
# このファイルは環境変数のテンプレートです。
# 実際の使用時は、このファイルを .env.frontend にコピーして
# 各値を実際の値に置き換えてください。
#
# コピー方法:
#   cp .env.frontend.example .env.frontend
#   # その後、.env.frontend を編集して実際の値を設定
#
# 注意: 
# - .env.frontend は .gitignore で除外されているため、
#   機密情報を含めても安全です。
# - Viteでは、環境変数は VITE_ プレフィックスが必要です。
# - 環境変数を変更した後は、開発サーバーを再起動してください。
# ============================================

# API Configuration
# VITE_API_BASE_URL: バックエンドAPIのベースURL
# 開発環境: http://localhost:8000/api/v1
# 本番環境: 実際のAPIサーバーのURLに変更
VITE_API_BASE_URL=http://localhost:8000/api/v1

# VITE_WS_URL: WebSocketサーバーのURL（将来の機能拡張用）
# 開発環境: ws://localhost:8000
# 本番環境: wss://your-domain.com（HTTPSの場合はwssを使用）
VITE_WS_URL=ws://localhost:8000

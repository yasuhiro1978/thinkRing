# ============================================
# thinkRing Backend Environment Variables
# ============================================
# このファイルは環境変数のテンプレートです。
# 実際の使用時は、このファイルを .env.backend にコピーして
# 各値を実際の値に置き換えてください。
#
# コピー方法:
#   cp .env.backend.example .env.backend
#   # その後、.env.backend を編集して実際の値を設定
#
# 注意: .env.backend は .gitignore で除外されているため、
#       機密情報を含めても安全です。
# ============================================

# Django Settings
# SECRET_KEY: Djangoのセキュリティキー。本番環境では必ず変更してください。
# 生成方法: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-in-production

# DEBUG: デバッグモードの有効/無効。本番環境では必ず False に設定してください。
DEBUG=True

# ALLOWED_HOSTS: アクセスを許可するホスト名。カンマ区切りで複数指定可能。
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# PostgreSQLデータベースの接続情報
DB_NAME=thinkring_db
DB_USER=thinkring_user
# DB_PASSWORD: データベースのパスワード。本番環境では強力なパスワードを使用してください。
DB_PASSWORD=your-database-password-here
DB_HOST=db
DB_PORT=5432

# JWT Settings
# JWT_SECRET_KEY: JWTトークンの署名に使用するキー。SECRET_KEYとは別の値を使用することを推奨。
# 本番環境では必ず変更してください。
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production

# JWT_ACCESS_TOKEN_LIFETIME: アクセストークンの有効期限（分）
JWT_ACCESS_TOKEN_LIFETIME=15

# JWT_REFRESH_TOKEN_LIFETIME: リフレッシュトークンの有効期限（日）
JWT_REFRESH_TOKEN_LIFETIME=7

# CORS Settings
# CORS_ALLOWED_ORIGINS: クロスオリジンリクエストを許可するオリジン。
# フロントエンドのURLをカンマ区切りで指定してください。
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
